const path = require("path");
require("dotenv").config();

module.exports = {
  env: "development", // production
  config: path.resolve(
    "src",
    "schemas",
    "implementations",
    "Sequelize",
    "config.ts"
  ),
};
